# TrustlessJanice Gate 0 Results

> Generated by `packages/contracts/scripts/measure-trustless-janice.ts` — do not edit by hand.
> Measured: 2026-06-06T10:04:40.997Z
> Network: hardhat (chainId 31337) + somniaTestnet manifest (T4 live runner cost)

## T2 — maxIterations overflow behavior

| Field | Value |
|-------|-------|
| Payload values accepted by Agents API (staticCall) | 1, 8, 16, 255 |
| Contract `MAX_JANICE_ITERATIONS` | 8 |
| ABI `uint8` max | 255 |
| Note | Local mock accepts uint8 values 1–255. Live Somnia Agents API uses same encoding; contract enforces loop cap at 8. |

**Conclusion:** Ship `MAX_JANICE_ITERATIONS = 8` in shared/constants and AgentOrchestrator. Payload may encode higher uint8 values but the contract loop aborts at 8.

## T3 — Gas per trustless tool-call round trip

| Metric | Gas |
|--------|-----|
| `createTrustlessTask` (incl. first Janice request) | 549,566 |
| Step callback + settle (mock fulfill) | 383,980 |
| Combined create + first step round trip | 519,048 |
| `resumeTrustlessTask` | 201,749 |

Measured on local Hardhat with MockAgentsApi: Janice hireSubAgent callback + oracle step settle + resumeTrustlessTask.

## T4 — STT charged per Janice iteration

| Field | Value |
|-------|-------|
| `getRequestDeposit()` | 0.03 STT (30000000000000000 wei) |
| Janice runner `costWei` | 0.07 STT (from `somniaTestnet.json` native agent seed) |
| **Cost per iteration** | **0.24 STT** (240000000000000000 wei) |

Formula: `deposit (0.03) + runnerCost (0.07) × SUBCOMMITTEE_SIZE (3) = 0.24 STT` per Janice iteration.

## Shipped constants

| Constant | Value | Rationale |
|----------|-------|-----------|
| `MAX_JANICE_ITERATIONS` | 8 | Contract + Gate 0 T2 |
| `minBudgetMultiplier` | 2 | Covers 2 Janice rounds minimum |
| `janiceRoundBufferMultiplier` | 3 | Matches `SUBCOMMITTEE_SIZE` native lane |
| Min budget formula | `minBudgetWei = janiceCostWei * 2, janiceCostWei = deposit + runnerCost * 3 (0.24 STT/iter on mock)` | Used in backend preflight |

## Gate 0 exit

- [x] Concrete numbers committed
- [x] `MAX_JANICE_ITERATIONS` set from measurements
- [x] Preflight budget defaults reference measured iteration cost
