# @twiin/frontend

React 19 + Vite + wagmi. See [../../README.md](../../README.md) and [../../build-context.md](../../build-context.md).

```bash
pnpm dev          # :5173, proxies /api → :3001
pnpm build
```

Optional: `VITE_WC_PROJECT_ID`, `VITE_PLAN_SECRET` (must match backend).
